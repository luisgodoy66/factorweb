<div class="modal-dialog modal-lg">
    <div class="modal-content">

            <div class="card">

                {% csrf_token %}
                <div class="card-header">
                    <strong>Detalle de cartera</strong>
                </div>
                
                <!-- body -->
                <div class="card-body card-block">
                    <table class="table table-striped table-bordered" id="bootstrap-data-table-export">
                        <thead>
                            <th>Deudor</th>
                            <th>Vencimiento</th>
                            <th>Documento</th>
                            <th>Saldo</th>
                        </thead>
                        <tbody>
                            {% for sublist in detalle %}
                                {% if sublist %} {# Ignora elementos vacíos en la lista principal #}
                                    {% for item in sublist %}
                                    <tr>
                                        <td>{{ item.deudor }}</td>
                                        <td>{{ item.vencimiento_str }}</td>
                                        <td>{{ item.documento_negociado }}</td>
                                        <td>{{ item.saldo }}</td>
                                    </tr>
                                    {% endfor %}
                                {% endif %}
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                <!-- footer -->
                <div class="card-footer">
                    <!-- <button id="button" class="btn btn-secondary">updateCell</button> -->
                    <button type="buttom" class="btn btn-danger btn-sm" 
                        onclick="return CerrarModal()">
                        <i class="fa fa-dot-circle-o"></i> Cerrar
                    </button>
                </div>
            </div>
      
    </div>

    <script>

    </script>
</div>